# Example of a smaller candidate set: route between just two cheap models.
name: cheap-pair
description: Only the two cheapest models; escalate to flash when lite is unsure.
completion_threshold: 0.7
lambda_cost: 1.0
max_k: 1
models:
  - google/gemini-3.1-flash-lite
  - deepseek-v4-flash
